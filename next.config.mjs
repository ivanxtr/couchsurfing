/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ht-assets.couchsurfing.com', 'cdn-icons-png.flaticon.com', 'images.unsplash.com'],
    },
};

export default nextConfig;
